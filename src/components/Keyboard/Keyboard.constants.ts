import { KeyboardLayout } from './Keyboard.types.ts';

export const KEYBOARD_LAYOUT: KeyboardLayout = [
    ['escape', 'q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p', 'backspace'],
    ['a', 's', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'enter'],
    ['z', 'x', 'c', 'v', 'b', 'n', 'm'],
] as const;
